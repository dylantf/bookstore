type Author {
  id: ID!
  name: String!
  books: [Book!]!
}

type Book {
  id: ID!
  title: String!
  published: Int!
  authorId: ID!
  author: Author!
}

type Query {
  authors: [Author!]!
  books: [Book!]!
}
